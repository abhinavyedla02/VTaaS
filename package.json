{
  "private": true,
  "workspaces": [
    "packages/*",
    "api",
    "worker"
  ],
  "scripts": {
    "api:build": "docker compose build api",
    "api:up": "docker compose up -d api",
    "api:logs": "docker logs -f vtaas_api"
  }
}